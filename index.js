/*
Variables defined in the test suite:
- oldID
- currentAge

You should NOT try to redefine any of the above variables.
*/

// Write your code below this line!
let newID = 1000000000+oldID
// Create a variable named newID that adds 1000000000 to the value in oldID, which you shouldn't define — it will be provided for you in the test suite.
let ageIsValid = Number.isInteger(currentAge)
// checks whether the value in currentAge is a valid integer.

let randomNumber = ((Math.random() * 10 )+ 10)
// randomNumber, which should contain a randomly-generated number between
//   0 (inclusive) and 20 (exclusive).
let randomInteger = Math.floor(randomNumber)
// randomInteger, which should take the value in randomNumber and
//  round it down to the nearest integer.
randomUserID = 1000000000 + randomInteger
// randomUserID, which should convert randomInteger into a valid ID number —
// an integer between 1000000001 and 1000000020.
